# Reusable Workflows Demo

This directory triggers the reusable workflow demos when changes are pushed.

## Demos
- Simple: Basic reusable workflow call
- Advanced: Reusable workflow with inputs, outputs, and secrets

See workflow files in `.github/workflows/demo-reusable-*.yml` and `.github/workflows/reusable-*.yml`
