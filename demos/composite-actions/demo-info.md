# Composite Actions Demo

This directory triggers the composite action demos when changes are pushed.

## Demos
- Simple: Basic composite action for Node setup
- Advanced: Composite action with inputs, outputs, and caching

See workflow files in `.github/workflows/demo-composite-*.yml` and action definitions in `.github/actions/`
